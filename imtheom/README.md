# IMTHEOM

This folder contains the AI Governance Operating Model landing page, generated document page, annex pages, local scripts, and visual assets.

Controlling governance:

`/Users/macal/Documents/JHMACAL MASTER FOLDER/PRODUCTION_GOVERNANCE_STANDARD.md`

Brand rule: ivory is banned. Web backgrounds for this surface use navy where dark works and the grey-blue body system where the document needs a light interior background.

Do not push, deploy, publish, promote, or replace the live page unless Julio approves after rendered proof.

Required local validation:

`python3 scripts/validate-production-governance.py imtheom/document/index.html --render-dir /path/to/render/proofs --touched-dir imtheom`
